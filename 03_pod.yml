apiVersion: v1
kind: Pod
metadata:
    name: testpod03
    annotations:
        description: Demo for Container Environment Variables
spec:
    containers:
        - name: c01
          image: ubuntu
          command: ["/bin/bash", "-c", "while true; do echo 'I am container c01'; sleep 5; done"]
          env:
              - name: FIRST_NAME
                value: Lokesh
              - name: LAST_NAME
                value: Mittal
