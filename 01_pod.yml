apiVersion: v1

kind: Pod

metadata:
    name: test-pod

    # annotations can have any key and value strings
    annotations:
        description: The annotations are for comment purpose only, and can contain any key-value pair.

spec:
    containers:
        - name: c01
          image: ubuntu
          command: ["/bin/bash", "-c", "while true; do echo Hello-Lokesh; sleep 5; done"]
    restartPolicy: Never
